-- Create the application database (MySQL).
-- Safe to run multiple times: CREATE DATABASE IF NOT EXISTS.
-- The ensure-database script uses DB_NAME from .env; for manual run replace below with your database name.

-- CREATE DATABASE IF NOT EXISTS `tb_wallet`;
