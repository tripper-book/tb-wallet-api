export enum CreditRequestStatus {
  PENDING_APPROVAL = 'pending_approval',
  APPROVED = 'approved',
  REJECTED = 'rejected',
}

export enum CreditRequestLogAction {
  REQUESTED = 'requested',
  APPROVED = 'approved',
  REJECTED = 'rejected',
}
